# Legacy Web Prototype

这里保存的是 SteamLinkMatcher 的旧 Python 网页原型，仅作历史参考。

当前主线版本在：

```text
../desktop/
```

如果仍需运行旧版原型：

```powershell
cd legacy-web
python -B server.py
```

然后打开：

```text
http://127.0.0.1:8765/
```

旧版缓存默认位于：

```text
legacy-web/data/cache.sqlite3
```

后续新功能默认不再同步到这个旧版原型。
