# Steam 游戏链接匹配工具需求文档

## 1. 项目背景

用户会提供一批游戏名称，软件需要自动匹配每个名称对应的 Steam 商店链接，并输出可核查、可导出的结果。

输入的游戏名称可能存在以下情况：

- 与 Steam 商店名称完全一致，例如 `Corpse Keeper`。
- 包含额外备注、日期、到期信息、括号内容，例如 `Warhammer 40,000: Rogue Trader(2026.7.1到期）`。这些信息只用于清洗名称，不作为第一版正式输出字段。
- 存在标点、大小写、空格、中文括号、英文括号等格式差异。
- 存在简称、别名、版本名、合集名、DLC 或非 Steam 游戏等模糊情况。

软件的核心目标不是只做字符串搜索，而是尽可能找到“用户实际想要的那款 Steam 游戏本体”。当结果不确定时，软件仍自动选择最像的结果作为推荐链接，但需要明确标记低置信度，方便用户人工筛查和修正。

## 2. 产品目标

### 2.1 核心目标

- 支持批量输入游戏名称。
- 第一版做成本地网页工具。
- 自动清洗游戏名称中的无关信息。
- 自动搜索并匹配 Steam 游戏页面。
- 输出每条输入对应的 Steam 链接。
- 对低置信度或多候选结果自动给出推荐结果，并标记为需要复核。
- 支持复制全部最终链接。
- 支持导出明细结果。
- 支持中文游戏名匹配英文 Steam 页面。

### 2.2 非目标

第一阶段暂不要求：

- 自动购买、打开 Steam 客户端或登录用户账号。
- 判断用户是否已拥有游戏。
- 判断游戏是否在用户所在地区可购买。
- 实时追踪价格、折扣、史低价格。
- 自动识别 Epic、GOG、Xbox、PlayStation 等非 Steam 平台链接。
- 把到期时间作为正式字段进行解析和导出。
- 匹配 DLC、合集包、Demo、Soundtrack 等非游戏本体页面。

这些能力可以作为后续扩展。

## 3. 目标用户

- 需要整理大量游戏清单的人。
- 需要把活动、兑换、库存、表格中的游戏名转换为 Steam 链接的人。
- 需要检查一批游戏是否存在 Steam 页面的人。

## 4. 使用场景

### 4.1 精准匹配

输入：

```text
Corpse Keeper
```

输出：

```text
Corpse Keeper -> https://store.steampowered.com/app/1601740/Corpse_Keeper/
```

### 4.2 带备注信息的匹配

输入：

```text
Warhammer 40,000: Rogue Trader(2026.7.1到期）
```

软件应先识别并移除无关备注：

```text
Warhammer 40,000: Rogue Trader
```

再匹配到：

```text
https://store.steampowered.com/app/2186680/Warhammer_40000/
```

### 4.3 模糊匹配

输入名称可能与 Steam 页面标题不完全一致，软件需要根据搜索结果、标题相似度、App ID、类型等信息给出最佳候选。

### 4.4 无结果或不确定

如果无法高置信度匹配，软件仍应自动选择最像的候选作为推荐结果，但必须明显标记为需要复核。

软件应输出：

- 原始输入。
- 清洗后的名称。
- 匹配状态。
- 自动推荐的 Steam 链接。
- 可能的候选链接。
- 需要人工复核的提示。

## 5. 功能需求

## 5.1 输入管理

软件应支持以下输入方式：

- 手动粘贴多行游戏名称。
- 从 `.txt` 文件导入。
- 从 `.csv` 文件导入。
- 从 `.xlsx` 文件导入。

每行或每个单元格代表一个待匹配游戏名称。

输入示例：

```text
Corpse Keeper
Warhammer 40,000: Rogue Trader(2026.7.1到期）
Hollow Knight
Sid Meier's Civilization VI - Platinum Edition
```

### 5.1.1 输入字段

最小输入字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 原始游戏名称 | 是 | 用户提供的原始文本 |

可选输入字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 备注 | 否 | 用户自定义备注 |
| 来源 | 否 | 例如 Humble、Fanatical、表格、活动名称 |

## 5.2 名称清洗

软件应在匹配前对游戏名称进行清洗，但保留原始输入。

### 5.2.1 清洗规则

应支持：

- 去除首尾空格。
- 统一中英文括号。
- 移除括号中的明显备注，例如：
  - `(2026.7.1到期）`
  - `(expires 2026-07-01)`
  - `(Steam)`
  - `(key)`
  - `(CN)`
- 保留可能属于正式游戏名的括号内容，例如：
  - `Shadow Tactics: Blades of the Shogun - Aiko's Choice`
  - `NieR:Automata`
  - `S.T.A.L.K.E.R. 2: Heart of Chornobyl`
- 统一多余空格。
- 处理特殊符号差异，例如中文冒号、全角逗号、英文逗号。

### 5.2.2 备注信息处理

如果原始名称中包含到期信息、平台说明、来源说明等备注，软件只需要将其从匹配用名称中移除。第一版不需要把到期时间解析为正式字段，也不需要在结果中展示或导出。

示例：

```text
Warhammer 40,000: Rogue Trader(2026.7.1到期）
```

解析结果：

| 字段 | 值 |
| --- | --- |
| 原始名称 | Warhammer 40,000: Rogue Trader(2026.7.1到期） |
| 清洗后名称 | Warhammer 40,000: Rogue Trader |
| 备注处理 | 移除括号内到期信息，不作为输出字段 |

## 5.3 Steam 匹配

### 5.3.1 匹配来源

优先考虑以下数据来源：

1. Steam 官方搜索页面。
2. Steam Store API 或可公开访问的接口。
3. 本地缓存的 Steam App 列表。
4. 必要时通过网页抓取 Steam 搜索结果。

具体实现时应优先使用稳定、合规、低频请求的方式，并做好缓存，避免重复请求。

### 5.3.2 匹配逻辑

匹配流程建议如下：

1. 使用原始名称搜索。
2. 使用清洗后名称搜索。
3. 如果输入是中文游戏名，尝试通过 Steam 搜索、别名库或候选标题信息匹配英文页面。
4. 对候选结果计算相似度。
5. 排除明显不是游戏本体的结果，例如 DLC、Soundtrack、Demo、Tool、合集包。
6. 无论置信度高低，只要存在候选，默认选出最像的一个作为推荐结果。
7. 如果最高候选置信度足够高，标记为“已匹配”。
8. 如果最高候选置信度不够高，或多个候选接近，标记为“已推荐，需复核”。
9. 如果无候选，标记为“未找到”。

### 5.3.3 候选结果字段

每个候选结果至少应包含：

| 字段 | 说明 |
| --- | --- |
| Steam App ID | Steam 应用 ID |
| Steam 标题 | Steam 页面标题 |
| Steam 链接 | 商店链接 |
| 类型 | 游戏本体、DLC、Demo、Soundtrack、合集包等 |
| 相似度分数 | 用于判断匹配可信度 |
| 置信度 | 高、中、低 |

## 5.4 匹配状态

每条输入应输出一个明确状态：

| 状态 | 说明 |
| --- | --- |
| 已匹配 | 软件高置信度找到唯一结果 |
| 已推荐，需复核 | 软件已自动选择最像的候选，但置信度不高或存在多个接近候选 |
| 未找到 | 没有找到可靠候选 |
| 可能非 Steam 游戏 | 搜索不到 Steam 页面，可能不在 Steam 上架 |
| 已人工修正 | 用户手动选择或粘贴了正确链接 |

## 5.5 人工复核

软件应提供人工复核能力。

用户可以：

- 查看原始输入和清洗后名称。
- 查看系统推荐的最佳结果。
- 查看多个候选 Steam 链接。
- 手动选择正确链接。
- 手动粘贴 Steam 链接。
- 标记为无 Steam 页面。
- 重新搜索。
- 对“已推荐，需复核”的结果进行筛查，有问题时直接修正。

人工确认后的结果应被保存，并可用于后续相同名称的自动匹配。

## 5.6 输出与导出

软件应支持两类输出：

- 一键复制全部最终 Steam 链接，每行一个链接。
- 导出包含完整匹配明细的文件。

明细导出格式：

- `.csv`
- `.xlsx`
- 可复制的 Markdown 表格，可作为辅助功能

### 5.6.1 输出字段

建议输出字段：

| 字段 | 说明 |
| --- | --- |
| 原始输入 | 用户提供的原始文本 |
| 清洗后名称 | 用于搜索的标准化名称 |
| 匹配状态 | 已匹配、已推荐需复核、未找到等 |
| Steam 游戏名 | 匹配到的 Steam 标题 |
| Steam App ID | 匹配到的 Steam App ID |
| Steam 链接 | 最终输出链接 |
| 置信度 | 高、中、低 |
| 候选数量 | 搜索到的候选个数 |
| 是否需要复核 | 是、否 |
| 结果来源 | 自动匹配、自动推荐、人工修正、手动粘贴 |
| 备注 | 用户原始备注或系统备注 |

### 5.6.2 输出示例

| 原始输入 | 清洗后名称 | 匹配状态 | Steam 游戏名 | Steam App ID | Steam 链接 | 置信度 | 是否需要复核 | 结果来源 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Corpse Keeper | Corpse Keeper | 已匹配 | Corpse Keeper | 1601740 | https://store.steampowered.com/app/1601740/Corpse_Keeper/ | 高 | 否 | 自动匹配 |
| Warhammer 40,000: Rogue Trader(2026.7.1到期） | Warhammer 40,000: Rogue Trader | 已匹配 | Warhammer 40,000: Rogue Trader | 2186680 | https://store.steampowered.com/app/2186680/Warhammer_40000/ | 高 | 否 | 自动匹配 |

### 5.6.3 复制全部链接

结果页应提供“复制全部链接”功能。

复制内容只包含最终 Steam 链接，每行一个：

```text
https://store.steampowered.com/app/1601740/Corpse_Keeper/
https://store.steampowered.com/app/2186680/Warhammer_40000/
```

如果某条记录未找到链接，则复制时默认跳过该条。明细导出中仍保留未找到记录。

## 6. 非功能需求

## 6.1 准确性

- 对完全匹配名称，应尽量自动输出正确链接。
- 对带备注的名称，应优先正确移除备注后再匹配。
- 对不确定结果，应自动选择最像的候选，并明确标记为需要复核。
- 第一版只把 Steam 游戏本体作为有效最终结果。
- 中文游戏名应尽量匹配到对应的英文 Steam 页面。

## 6.2 性能

建议目标：

- 100 条游戏名称在合理网络条件下可在数分钟内完成。
- 已缓存结果应接近即时返回。
- 批量任务应显示进度。

## 6.3 稳定性

- 网络失败时应自动重试。
- Steam 搜索临时失败时应记录失败原因。
- 任务中断后应能保留已完成结果。

## 6.4 可维护性

- 名称清洗规则应可配置。
- 匹配阈值应可调整。
- 缓存应可更新或清空。
- 日志应便于排查错误匹配。

## 7. 数据存储

建议至少保存以下数据：

- 输入任务记录。
- 原始输入列表。
- 清洗后名称。
- 搜索候选结果。
- 最终确认结果。
- 人工修正记录。
- 本地缓存的名称到 Steam App ID 映射。

## 8. 推荐技术方案

第一版确定做成本地网页工具。

### 8.1 本地 Web 工具

适合场景：

- 需要批量导入导出。
- 需要表格化复核。
- 后续可能增加可视化界面。

建议结构：

- 前端：React 或 Vue。
- 后端：Python FastAPI 或 Node.js。
- 数据存储：SQLite。
- 导入导出：CSV、XLSX。
- 匹配逻辑：后端服务统一处理。

### 8.2 命令行工具

命令行工具不作为第一版核心形态，可作为后续扩展。适合场景：

- 只需要快速批量转换。
- 输入输出主要依赖文件。

建议命令示例：

```bash
steam-link-match input.xlsx --output result.xlsx
```

### 8.3 第一阶段建议

建议先做“本地 Web 工具 + SQLite 缓存”：

- 操作门槛低。
- 方便人工确认。
- 适合处理模糊匹配。
- 后续可以继续封装成桌面软件。

### 8.4 缓存说明

缓存的意思是：软件把已经查过、确认过的匹配结果保存在本地数据库中，之后遇到相同或高度相似的游戏名时，可以直接复用结果，减少联网搜索和重复人工修正。

典型场景：

- 第一次输入 `Corpse Keeper`，软件联网搜索并确认 App ID 为 `1601740`。
- 之后再次输入 `Corpse Keeper`，软件直接从本地缓存返回链接，不必再查 Steam。
- 如果用户人工把某个中文名修正为正确 Steam 链接，例如 `全面战争：战锤3` -> 对应英文 Steam 页面，软件以后可以优先记住这个人工修正。

第一版建议启用缓存，因为它能提升速度，也能让人工修正越来越有价值。缓存应提供清空或重建入口，避免错误结果长期保留。

## 9. 页面与交互草案

### 9.1 主页面

主页面包含：

- 输入区：粘贴多行游戏名称。
- 导入按钮：上传 `.txt`、`.csv`、`.xlsx`。
- 开始匹配按钮。
- 任务进度。
- 结果表格。
- 复制全部链接按钮。
- 导出按钮。

### 9.2 结果表格

表格列：

- 原始输入。
- 清洗后名称。
- 匹配状态。
- 推荐 Steam 游戏名。
- Steam 链接。
- 置信度。
- 是否需要复核。
- 结果来源。
- 操作。

操作包括：

- 打开 Steam 页面。
- 查看候选。
- 手动修改。
- 确认。
- 标记未找到。

### 9.3 候选确认弹窗

当结果为“已推荐，需复核”时，用户可以打开候选弹窗。

弹窗显示：

- 原始输入。
- 清洗后名称。
- 推荐候选列表。
- 每个候选的标题、App ID、链接、类型、相似度。
- 手动输入链接区域。

## 10. 匹配规则细节

## 10.1 自动确认条件

当满足以下条件时，可以自动标记为“已匹配”：

- 候选标题与清洗后名称高度相似。
- 候选类型是游戏本体。
- 第一候选明显优于第二候选。
- Steam App ID 有效。

## 10.2 复核条件

以下情况应标记为“已推荐，需复核”：

- 多个候选标题相似。
- 搜索结果包含游戏本体、DLC、合集、Demo 等多个可能结果。
- 清洗规则移除了较多文本，存在误删风险。
- 标题相似度中等，但存在一个相对最像的候选。

在该状态下，软件仍输出自动推荐链接，但用户可以在筛查后手动修正。

## 10.3 未找到条件

以下情况可标记为“未找到”：

- Steam 搜索没有返回候选。
- 返回结果与输入名称明显无关。
- 网络正常但多次搜索均无有效结果。

## 11. 风险与难点

| 风险 | 说明 | 应对方式 |
| --- | --- | --- |
| Steam 搜索结果不稳定 | 搜索排序可能变化 | 使用缓存、相似度、人工复核 |
| 游戏名包含版本或合集 | 可能匹配到不同 App | 保留候选并提示确认 |
| DLC 与本体混淆 | DLC 也有独立 Steam 页面 | 第一版只接受游戏本体，低置信度时标记复核 |
| 中文游戏名匹配英文页面 | Steam 页面常用英文标题 | 支持别名库、候选相似度和人工修正缓存 |
| 中文备注复杂 | 备注格式多样 | 建立可配置清洗规则 |
| 部分游戏下架 | Steam 页面可能存在但搜索不到 | 支持手动链接和历史缓存 |
| 地区限制 | 不同地区看到的页面可能不同 | 记录搜索失败原因，不把地区判断作为第一版核心 |

## 12. 第一版验收标准

第一版完成后，应满足：

- 可以粘贴至少 100 条游戏名称并批量处理。
- 可以从 `.txt`、`.csv`、`.xlsx` 导入游戏名称。
- 可以识别并移除常见括号备注。
- 可以匹配 Steam 链接并输出 App ID。
- 可以区分已匹配、已推荐需复核、未找到、已人工修正。
- 匹配不确定时可以自动选择最像的候选，并标记需要复核。
- 可以人工选择候选结果或手动粘贴链接进行修正。
- 可以一键复制全部最终链接，每行一个。
- 可以导出 `.csv` 或 `.xlsx` 明细。
- 只把 Steam 游戏本体作为有效最终结果。
- 支持中文游戏名匹配英文 Steam 页面。
- 对示例 `Corpse Keeper` 能输出 `https://store.steampowered.com/app/1601740/Corpse_Keeper/`。
- 对示例 `Warhammer 40,000: Rogue Trader(2026.7.1到期）` 能先清洗为 `Warhammer 40,000: Rogue Trader`，再输出 `https://store.steampowered.com/app/2186680/Warhammer_40000/`。

## 13. 后续扩展

可选扩展方向：

- 支持自动从剪贴板读取。
- 支持拖拽文件导入。
- 支持 Steam 页面封面图展示。
- 支持价格、折扣、评价、发行日期等字段。
- 支持一键打开所有需复核候选。
- 支持记录人工修正后的别名库。
- 支持多语言标题匹配。
- 支持命令行批处理。
- 支持打包为 Windows 桌面应用。

## 14. 已确认需求

基于用户反馈，第一版已确认：

1. 软件形态：本地网页工具。
2. 输入方式：支持手动粘贴、`.txt`、`.csv`、`.xlsx`。
3. 输出方式：支持一键复制全部最终 Steam 链接，也支持导出 `.csv` 或 `.xlsx` 明细。
4. 不确定匹配：软件自动选择最像的候选，同时标记为需要复核；用户筛查后可以人工修正。
5. 到期时间：不作为正式字段，不解析、不展示、不导出；只在名称清洗时移除相关备注。
6. 匹配范围：只匹配 Steam 游戏本体，不把 DLC、合集、Soundtrack、Demo 作为最终有效结果。
7. 语言支持：支持中文游戏名匹配英文 Steam 页面。
8. 缓存策略：第一版默认启用本地缓存，用于保存已查过和人工修正过的结果；缓存需支持清空或重建。
9. 中文名匹配策略：第一版先完全依赖 Steam 搜索和人工修正积累，不内置中文别名库。
10. Excel 导入策略：`.xlsx` 默认读取第一列作为游戏名。

## 15. 待进一步确认问题

暂无。当前需求已足够进入技术设计和第一版开发。
